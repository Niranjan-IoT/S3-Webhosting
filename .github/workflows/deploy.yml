name: Deploy to S3 + CloudFront

on:
  push:
    branches: [main]       # trigger on push to main only
  workflow_dispatch:       # also allow manual run from GitHub UI

env:
  AWS_REGION: ap-south-1         # your bucket's region
  S3_BUCKET: my-bucket-value-page  # replace with your bucket name
  CF_DIST_ID: E1U2BWLCLVMD58  # replace with your distribution ID

jobs:
  deploy:
    name: Deploy
    runs-on: ubuntu-latest   # fresh VM every run

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ env.AWS_REGION }}

      - name: Deploy to S3
        run: |
          aws s3 sync . s3://${{ env.S3_BUCKET }}/ \
            --delete \
            --exclude ".git/*" \
            --exclude ".github/*" \
            --exclude "*.md" \
            --cache-control "public, max-age=3600"

      - name: Invalidate CloudFront
        run: |
          aws cloudfront create-invalidation \
            --distribution-id ${{ env.CF_DIST_ID }} \
            --paths "/*"

      - name: Done
        run: echo "Deployed to https://niruclouddev.in"